3. Histórico de Implementações (Passo a Passo)
Fase 1: Estrutura Base e Seleção de Perfil
Criação do componente RoleSelection.tsx: Uma tela de boas-vindas que define se o usuário é um Cliente ou um Profissional.

Implementação do estado userRole no App.tsx para gerenciar a navegação condicional.

Fase 2: Interface do Cliente (Busca e Filtros)
Criação do componente CustomerViews.tsx: Centraliza a lógica de exibição de profissionais.

Implementação de filtros de busca em tempo real por nome e categoria de serviço.

Desenvolvimento da lógica de ícones dinâmicos (getCategoryIcon) para categorias como Elétrica, Hidráulica e Pintura.

Integração com o backend para listar prestadores reais salvos no banco de dados.

Fase 3: Painel do Prestador (Gestão e Dashboard)
Criação do componente ProviderView.tsx: O centro de controle do profissional.

Modularização com StatCard.tsx: Desenvolvimento de um componente reutilizável para exibir estatísticas cruciais como:

Ganhos no Mês (Foco financeiro).

Avaliação Média (Reputação).

Serviços do Dia (Gestão de tempo).

---------------------10/07/2026------------------------------
          CORRIGINDO ERROS DE SEGURANCA
Corrigindo problema de Senha em texto puro, isso impede de alguem entrar no banco e ver todas as senhas...

1. instale a biblioteca e os tipos dela para o TypeScript rodando o seguinte comando:
npm install bcrypt && npm install --save-dev @types/bcrypt

---------------------12/07/2026------------------------------
2. No server.ts, importamos a biblioteca bcrypt para lidar com a segurança das senhas.

3. Atualizamos a rota POST /cadastro-profissional:
   - Passamos a interceptar a senha em texto limpo vinda do front-end.
   - Usamos o comando 'await bcrypt.hash(senha, 10)' para gerar um hash criptografado.
   - Salvamos apenas esse hash na tabela de profissionais do PostgreSQL, protegendo o dado original.

4. Atualizamos a rota POST /login-profissional:
   - Paramos de comparar a senha diretamente com operadores comuns (=== ou !==).
   - Passamos a utilizar o 'await bcrypt.compare(senha, profissional.senha)' para decodificar e validar o hash gerado de forma segura.

5. Estruturação Inicial do Painel do Administrador (AdminView):
   - Criado o componente funcional 'AdminView.tsx' na pasta de componentes para gerenciar o ecossistema do app.
   - Adicionado um botão de atalho discreto e flutuante na tela de seleção inicial (RoleSelection.tsx) com a classe '.btn-atalho-admin'.
   - Atualizado o 'App.tsx' com a condicional de renderização baseada no papel do usuário (userRole === 3) para exibir o painel de controle.
   - Refatorado e centralizado os novos estilos utilitários do admin dentro do arquivo 'index.css' na camada de componentes do Tailwind.

-------------------13/07/2026-------------------
6. Renovação Visual da Tela de Seleção (UI/UX):
   - Criado padrão de ícones em marca d'água no Canva e salvo na pasta 'public'.
   - Criada a classe '.bg-selecao-pattern' no 'index.css' com fundo escuro e repetição responsiva.
   - Atualizado o 'RoleSelection.tsx' para usar a nova classe, eliminando estilos inline.

7. Configuração de Teste Mobile e Ajustes de Integração com Banco:
   - Configurado o Vite com a flag '--host' para habilitar o acesso ao app via rede local pelo celular.
   - Atualizados os endpoints de fetch do frontend para apontar para o IP local (192.168.1.12), corrigindo erros de conexão e permitindo autenticação e busca direto em dispositivos móveis.
   - Validada a criptografia de senhas (bcrypt) em novos cadastros e corrigidos os critérios de busca integrados ao banco PostgreSQL.
   
-------------------20/07/2026-------------------
8. Resolução de Conexão Mobile, Autenticação e Segurança do Git:
   - Identificado e corrigido o erro de autenticação/login causado por senhas sem hash no PostgreSQL e falha de apontamento no backend.
   - Ajustados os endpoints de busca e login para utilizar o IP da máquina local (192.168.1.12), permitindo a sincronização e pesquisa de profissionais em tempo real via celular.
   - Configurada a regra de segurança no arquivo '.gitignore' para a pasta 'Documentacao/comandosTerminal'.
   - Executada a remoção do arquivo de anotações pessoais do rastreamento do Git ('git rm --cached'), excluindo-o do repositório remoto do GitHub e mantendo-o salvo apenas localmente. 

-------------------22/07/2026-------------------
9. Pivotagem de Modelo de Negócio (Estilo GetNinjas - Venda de Leads):
   - Definida a estratégia de monetização baseada em compra de contatos/leads via moeda virtual para evitar a "fuga de plataforma" e eliminar custos de gateways com retenção (escrow).
   - Ajustada a modelagem do banco de dados PostgreSQL:
     * Adicionada a coluna 'saldo_moedas' na tabela 'profissionais' (iniciando com bônus de 50 moedas de boas-vindas).
     * Criada a tabela 'pedidos_servico' para armazenar solicitações enviadas pelos clientes (com limite de 3 respostas por pedido).
     * Criada a tabela 'leads_comprados' para controle de histórico de compra de contatos por profissional.

10. Atualização do Backend (server.ts) e Segurança de Dependências:
    - Adicionadas as novas rotas de API:
      * 'POST /pedidos-servico': Permite ao cliente publicar uma necessidade na plataforma.
      * 'GET /pedidos-servico': Exibe o mural de oportunidades abertas para os prestadores.
      * 'POST /comprar-lead': Realiza a transação com trava de segurança (verifica saldo de moedas, debita a taxa, trava o limite de 3 prestadores e desbloqueia o WhatsApp do cliente).
    - Substituído o pacote 'bcrypt' nativo por 'bcryptjs' para garantir compatibilidade cross-platform e evitar travamentos no ambiente Windows/Node.js.
    - Configurado o servidor no IP local (0.0.0.0:5000) garantindo a comunicação fluida com o app mobile.

11. Atualização da Interface e Fluxo de Telas (UI/UX):
    - Refatorado o 'ProviderView.tsx' (Mural do Prestador):
      * Exibição do saldo de moedas em tempo real no topo da tela.
      * Cards de oportunidades dinâmicos com botão "Desbloquear Contato".
      * Revelação instantânea do WhatsApp do cliente com direcionamento direto para conversa no app após a compra do lead.
    - Refatorado o 'CustomerViews.tsx' (Visão do Cliente):
      * Adicionado banner de destaque e botão para "Solicitar Orçamento".
      * Criado o modal interativo 'ModalNovoPedido' para o cliente cadastrar demandas rapidamente, alimentando o mural do prestador ao vivo.        

-------------------28/07/2026-------------------
12. Refatoração Arquitetural e Modularização do Backend (Express + TypeScript):
    - Dividida a estrutura monolítica do 'server.ts' em módulos independentes e organizados utilizando 'express.Router()':
      * Criada a pasta 'src/config/db.ts' para centralizar a conexão e o gerenciamento de Pool do PostgreSQL com tratamento seguro de tipos e fallback para '.env'.
      * Criado 'src/routes/auth.routes.ts' isolando o fluxo de cadastro, login com 'bcryptjs' e a rota 'GET /profissionais' para listagem no mural inicial.
      * Criado 'src/routes/pedidos.routes.ts' agrupando a publicação de ordens de serviço, mural de oportunidades e o desconto de saldo para compra de leads.
      * Criado 'src/routes/moedas.routes.ts' para centralizar a inteligência de cobrança de pacotes de moedas.
      * novo e simplificado o 'src/server.ts' para atuar puramente como ponto de entrada limpo e inicialização de middlewares globais (CORS e express.json).

13. Estruturação do Sistema de Recarga de Moedas e Ajustes de Banco:
    - Criada a nova tabela 'transacoes_moedas' no PostgreSQL para registro de intenções e histórico financeiro de recargas (status PENDENTE/APROVADO).
    - Criadas as rotas no backend para comercialização de créditos:
      * 'POST /gerar-pix-moedas': Gera a string Pix Copia e Cola formatada nos padrões do Banco Central para o valor do pacote selecionado.
      * 'POST /confirmar-recarga': Atualiza a transação para APROVADO e soma atomicamente as novas moedas ao 'saldo_moedas' do prestador.

14. Correção de Rotas e Resolução de Erros de Parse no Frontend:
    - Identificado e corrigido o erro 404/JSON SyntaxError no navegador ao buscar prestadores na tela do cliente.
    - Mapeada a rota 'GET /profissionais' no módulo de autenticação e reajustado o caminho relativo dos módulos dentro da pasta 'src/', restaurando a renderização dinâmica de cards no 'CustomerViews.tsx'.

15. Resolução de Conexões, Ajustes no Banco e Modal de Recarga PIX:
    - Atualizados todos os endpoints de fetch do frontend (`CustomerViews.tsx`, `RoleSelection.tsx`, `ProviderView.tsx`) para o novo IP local da rede (`192.168.1.5:5000`), eliminando erros de conexão.
    - Corrigida a divergência de nome de colunas no backend PostgreSQL (ajustada a requisição de inserção e consulta para utilizar a coluna 'especialidade' na tabela 'pedidos_servico').
    - Corrigidos erros de typings do TypeScript e ajuste de propriedade CSS de layout (`justifyContext` para `justifyContent`) no `ProviderView.tsx`.
    - Finalizada a interface do Modal de Recarga no `ProviderView.tsx`:
      * Adicionado o botão "+ Recarregar Moedas" no painel da carteira.
      * Criado fluxo interativo para escolha de pacotes (Iniciante 30, Profissional 70 e Mestre 150 moedas).
      * Implementada a exibição do código PIX Copia e Cola e o botão para simulação e confirmação imediata da recarga no saldo do prestador.

-------------------29/07/2026-------------------
16. Implementação de Autenticação com JWT e Proteção de Rotas:
    - Instalada e configurada a biblioteca 'jsonwebtoken' no backend Node.js/Express.
    - Atualizada a rota 'POST /login-profissional' em 'auth.routes.ts' para gerar e assinar um token JWT com validade de 24 horas.
    - Criado o middleware 'auth.middleware.ts' para interceptar, validar o token nos cabeçalhos 'Authorization' e garantir o acesso apenas a usuários autenticados.
    - Protegidas as rotas críticas no backend ('POST /comprar-lead' e 'POST /gerar-pix-moedas'), injetando com segurança o ID do profissional a partir do payload do token.
    - Atualizado o 'App.tsx' para salvar o token JWT no 'localStorage' no momento do login com sucesso.
    - Atualizado o 'ProviderView.tsx' para resgatar o token do 'localStorage' e enviá-lo nos cabeçalhos da requisição ('Bearer Token'), garantindo a segurança completa das transações de moedas e compra de leads.
-------------------04/09/2026-------------------
17. Redesign Visual da Tela Inicial (RoleSelection) e Identidade Orange:
    - Removidos cards brancos e títulos secundários poluídos, adotando foco minimalista em apenas dois botões centrais.
    - Definida nova paleta visual moderna com gradiente laranja e terracota (#f15a24 a #ce4d17).
    - Implementado layout estilo "pill" com os avatares 3D (cliente e castor construtor) vazando para fora dos cards usando posicionamento absoluto (`h-32`, `-top-7` e `-bottom-3`) e efeito `pointer-events-none`.
    - Ajustado o espaçamento responsivo para visualização limpa e intuitiva em smartphones.

18. Suporte a Portfólio de Serviços Realizados e Perfil no Banco:
    - Modeladas novas colunas e tabelas no PostgreSQL:
      * Adicionadas as colunas `foto_url` e `biografia` na tabela `profissionais`.
      * Criada a tabela relacional `servicos_realizados` com campos para `profissional_id`, `titulo`, `descricao`, `imagem_url` e `data_conclusao`.
      * Adicionado índice `idx_servicos_profissional` para otimização de leitura.
    - Criado o módulo de rotas `src/routes/portfolio.routes.ts` com endpoints protegidos via JWT para o prestador gerenciar seu portfólio e atualizar bio/foto.
    - Atualizada a consulta `GET /profissionais` em `auth.routes.ts` com subconsulta `json_agg` para retornar foto e histórico de trabalhos agregados diretamente em JSON.

-------------------05/09/2026-------------------
19. Reestruturação da Interface do Cliente (CustomerViews.tsx) e Regra de Negócio:
    - Padronizada a interface do cliente com o cabeçalho e degradê laranja institucional.
    - Atualizado o card do prestador com avatar/foto de perfil, especialidade, avaliação e contagem de serviços concluídos.
    - Criado o modal expansível de **Portfólio**, permitindo ao cliente inspecionar trabalhos anteriores com fotos e detalhes para validação de credibilidade.
    - Removido o botão de orçamento direto com o prestador para proteger o contato e centralizar as demandas exclusivamente no mural geral.
    - Implementado o modal de **Solicitação de Orçamento Geral** integrado com a API (`POST /pedidos-servico`), permitindo ao cliente registrar pedidos com nome, telefone, categoria e descrição do problema.

20. Proteção de Contato, Fluxo de Moedas e Mural do Prestador (ProviderView.tsx):
    - Atualizado o painel do prestador com o cabeçalho da carteira em tempo real e atalho para recarga Pix.
    - Implementada a trava de contato protegido: o telefone do cliente permanece mascarado no mural até que o profissional clique em "Liberar Contato" e confirme o débito de 15 moedas.
    - Consumida a rota autenticada `POST /comprar-lead`, enviando o token JWT via cabeçalho `Authorization: Bearer`.
    - Ao confirmar a transação, o sistema debita as moedas localmente e na API, liberando o botão com link direto para o WhatsApp do cliente (`wa.me`).
    - Integrado o modal de recarga com simulação de código Pix Copia e Cola (`POST /gerar-pix-moedas` e `POST /confirmar-recarga`).

21. Depuração de Rede, TypeScript e Persistência de Autenticação:
    - Atualizado o `server.ts` para escutar na interface `0.0.0.0:5000`, viabilizando o teste simultâneo via Wi-Fi pelo celular (`192.168.5.109:5000`).
    - Corrigido erro de tipagem no `App.tsx` referente às propriedades (`services`, `loading`, `searchTerm`, `onBack`) passadas ao componente `CustomerViews`.
    - Corrigido fechamento prematuro de bloco na função `handleLoginSucesso` no `App.tsx` e unificada a variável de estado `setServices`.
    - Resolvido erro de "Token inválido ou expirado" garantindo que o token JWT retornado no login seja persistido no `localStorage` antes da troca de rotas.
